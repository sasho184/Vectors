#include "Triangle.h"

Triangle::Triangle()
{
	Point A(0, 0, 0);
	Point B(0, 0, 0);
	Point C(0, 0, 0);

}

Triangle::Triangle(Point A , Point B, Point B)
{
	Point A = this->Point A;
	Point B = this->Point B;
	Point C = this->Point C;

}
